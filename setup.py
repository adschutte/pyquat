from setuptools import setup, find_packages

setup(name = "pyquat",
      version = "0.1",
      packages = find_packages(),
      install_requires = ['numpy'],
      description = "A python quaternion module.",
      author = "Aaron D Schutte",
      author_email = "aaron.d.schutte@aero.org",
      url = "n/a")
